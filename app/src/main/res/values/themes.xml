<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Change the parent theme to Theme.AppCompat -->
    <style name="Theme.TourneyPlayPal" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here -->
    </style>

    <!-- Define the SplashTheme with the app's theme as parent -->
    <style name="SplashTheme" parent="Theme.TourneyPlayPal">
        <item name="android:windowBackground">@drawable/splashscreen</item>
    </style>
</resources>
